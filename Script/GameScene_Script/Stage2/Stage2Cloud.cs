using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Bolt;
/*
public class Stage2Cloud : Photon.Bolt.EntityBehaviour<IStage2Mission>
{
    public override void Attached()
    {
        state.SetTransforms(state.Cloud, transform);

        state.AddCallback("Cloud", Moving);
    }

    void Moving()
    {

        entity.transform.position = state.Cloud.Position;
        entity.transform.rotation = state.Cloud.Rotation;

    }
}
*/
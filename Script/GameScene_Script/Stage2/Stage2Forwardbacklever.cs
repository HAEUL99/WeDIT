using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Bolt;

/*
public class Stage2Forwardbacklever : Photon.Bolt.EntityBehaviour<IStage2Mission>
{
    public override void Attached()
    {
        state.SetTransforms(state.Stage2forwardlever, transform);

        state.AddCallback("Stage2forwardlever", Moving);
    }

    void Moving()
    {

        entity.transform.position = state.Stage2forwardlever.Position;
        entity.transform.rotation = state.Stage2forwardlever.Rotation;

    }
}
*/

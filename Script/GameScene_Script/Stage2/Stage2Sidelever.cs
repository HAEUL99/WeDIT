using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Bolt;

/*
public class Stage2Sidelever : Photon.Bolt.EntityBehaviour<IStage2Mission>
{
    public override void Attached()
    {
        state.SetTransforms(state.Stage2sidelever, transform);

        state.AddCallback("Stage2sidelever", Moving);
    }

    void Moving()
    {

        entity.transform.position = state.Stage2sidelever.Position;
        entity.transform.rotation = state.Stage2sidelever.Rotation;

    }
}
*/

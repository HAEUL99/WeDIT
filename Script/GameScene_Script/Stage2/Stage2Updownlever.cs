using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Bolt;

/*
public class Stage2Updownlever : Photon.Bolt.EntityBehaviour<IStage2Mission>
{
    public override void Attached()
    {
        state.SetTransforms(state.Stage2uplever, transform);

        state.AddCallback("Stage2uplever", Moving);
    }

    void Moving()
    {

        entity.transform.position = state.Stage2uplever.Position;
        entity.transform.rotation = state.Stage2uplever.Rotation;

    }
}
*/
